#include <iostream>
#include <conio.h>
int main()
{
int num , row, col;

cout << "Enter the last number :";
cin >> num ;
    // printing space 
    for (  row = 2 ; row <= num-1 ; row++)
        {
            for (col = 1 ; col <= row ; col++)
            cout << " " << endl; 
        } 
   
   // printing number 
      for

}